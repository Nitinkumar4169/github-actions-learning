name: Secrets Demo

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:

      - name: Read Secrets
        run: |
          echo "Name: ${{ secrets.MY_NAME }}"
          echo "Role: ${{ secrets.MY_ROLE }}"
          echo "Message: ${{ secrets.SECRET_MESSAGE }}"
