name: Variables Demo

on:
  push:

env:
  APP_NAME: frappe
  SITE_NAME: apnacollege.test
  ENVIRONMENT: dev

jobs:

  test:

    runs-on: ubuntu-latest

    steps:

      - name: Print Variables

        run: |
          echo "Application: $APP_NAME"
          echo "Site: $SITE_NAME"
          echo "Environment: $ENVIRONMENT"
